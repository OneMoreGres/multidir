workspace:
  base: /base
  path: src
  
pipeline:
  test:
    image: gres/multidir:precise
    volumes:
      - /home/gres/Projects/filemanager/dist:/drone/dist
    commands:
      - cd /build
      - /base/src/utils/precise/build.sh
      - rename 's/(.*)\.appimage/$1-$DRONE_BRANCH\.appimage/' *
      - mv *.appimage /drone/dist
